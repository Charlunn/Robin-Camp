# Repository-wide environment template
# Copy this file to .env in the repository root if you want shell helpers to
# pick up default values, then sync the same values into app/.env when running
# docker compose.

# --- Service configuration ---
PORT=8080
AUTH_TOKEN=replace-with-static-token
DB_URL=postgres://cinema:cinema@db:5432/cinema?sslmode=disable
BOXOFFICE_URL=https://mock.apifox.com/m1/4288164-0-default
BOXOFFICE_API_KEY=replace-with-real-api-key

# Optional override for scripts that talk to the running API
BASE_URL=http://127.0.0.1:8080

# --- Docker/CI helpers ---
APP_IMAGE=cinema-app
POSTGRES_DB=cinema
POSTGRES_USER=cinema
POSTGRES_PASSWORD=cinema
POSTGRES_PORT=5432
FRONTEND_API_BASE_URL=

# Usage:
# 1. Copy this file to .env: cp .env.example .env
# 2. Copy app/.env.example to app/.env and make sure the values stay in sync
# 3. Start the stack from the app directory: (cd app && make docker-up)
# 4. Run the end-to-end tests: (cd app && make test-e2e)
